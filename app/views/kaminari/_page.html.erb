<% if page.current? %>
  <span class="bg-red-800 text-white px-4 py-3 rounded"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "px-4 text-gray-800 font-light mx-1" %>
<% end %>
